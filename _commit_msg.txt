feat: incremental pipeline with detailed progress — skip already-fetched data

- _run_pipeline_steps() now reports per-step progress via callbacks
  Watchlist + Backtest show: "📡 Kiểm tra & tải" → "🔄 Chuẩn hóa" → "🔗 Timeline" → "🏷️ Nhãn" → "⚙️ Feature"
- Collector: get_incremental_start() already skips data already downloaded
  Now shows "✓ Dữ liệu đã cập nhật (6/6 nguồn đã có) — bỏ qua tải" when nothing new
- Normalize: process_raw_to_parquet() already skips files with existing parquet
- DuckDB persists on disk (dev.duckdb) — labels/feature_results tables survive restart
- Views recreated each run (fast, in-memory) — no stale data
- Stop deleting dev.duckdb on app restart — preserves cached tables

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
