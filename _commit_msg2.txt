feat(incremental+UI): incremental download, pass live DB conn to runner, label/metric widgets

- Add get_latest_data_timestamp() and get_incremental_start() to pipeline.py:
  scan existing raw JSONL to find latest timestamp per data_type+symbol,
  adjust collector start_time so already-downloaded data is skipped
- Streamlit UI now calls get_incremental_start per collector and reports
  which data types were skipped vs freshly downloaded
- run_experiment() accepts an optional live DuckDB connection (conn=)
  to avoid file-lock conflicts when the UI already holds a write conn
- UI passes db.conn to run_experiment so metrics are real, not mock
- Add label distribution chart (3 metric cards) and warning when n_positive=0
- Add aggregate metrics display row in the UI

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
