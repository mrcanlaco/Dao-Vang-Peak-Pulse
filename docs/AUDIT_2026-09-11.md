# Rà soát Đảo Vàng — 11/09/2026

## Phạm vi và kết quả ban đầu

- Rà soát mã Python, React, cấu hình Docker, khởi động local, triển khai và các script chẩn đoán.
- Truy cập SSH GCP tại 136.110.29.208 và kiểm tra tên miền https://daovang.comaygiauco.com.
- Local và GCP cùng revision f240206a8ac5e81adc62295001782b54c6a9120e trước khi sửa.
- Cả 3 container đang chạy. Ổ đĩa hệ thống dùng 81% (còn khoảng 19 GB); dữ liệu ứng dụng khoảng 4.2 GB.
- Bộ kiểm thử ban đầu: 448 qua, 8 lỗi. Ruff: 17 lỗi. Frontend build qua, có 21 cảnh báo lint.
- API chính đọc được dữ liệu; scanner đang quét và heartbeat mới. Không gửi thử Telegram, không đặt lệnh, không thay đổi mô hình hoặc ngưỡng cảnh báo.

## Các lỗi đã sửa

| Mức | Vấn đề | Thay đổi |
|---|---|---|
| Nghiêm trọng | Endpoint tệp tĩnh đọc được tệp ngoài frontend khi chưa đăng nhập; đã kiểm chứng bằng pyproject.toml trên GCP | Giới hạn đường dẫn sau khi resolve, chặn đường dẫn mã hóa và dấu phân cách Windows |
| Cao | Mật khẩu SSH ghi cứng trong 12 script chẩn đoán cũ | Bỏ kết nối cũ trong check_public_url; 11 script còn lại đọc biến DAO_VANG_LEGACY_SSH_PASSWORD |
| Cao | Báo cáo API/UI tự thêm precision, lead time và kết luận kiểm định thành công khi thiếu số liệu | Dùng kết quả có thật; thiếu dữ liệu trả null; phân biệt báo cáo lịch sử với mô hình đang chạy |
| Cao | Nút ẩn tín hiệu gọi sai payload và cố ghi DuckDB chỉ đọc của scanner | Sửa payload; lưu trạng thái ẩn riêng, bền vững, không tranh khóa DuckDB |
| Vừa | API klines gọi hàm không tồn tại, trả 503 | Dùng chung xử lý biểu đồ, giới hạn số nến |
| Vừa | JSON sai định dạng hoặc Content-Length không hợp lệ làm hỏng request | Trả 400/413 rõ ràng; giới hạn nội dung 1 MiB; kiểm tra đăng nhập trước đọc nội dung |
| Vừa | Mật khẩu Unicode làm phép so sánh thất bại | So sánh byte UTF-8 theo cơ chế hmac |
| Vừa | Thêm/xóa watchlist gọi API không tồn tại | Đồng bộ frontend với /api/watchlist/add và /remove |
| Vừa | UI báo gửi thành công dù HTTP lỗi, báo quét xong 48 mã và tiến độ tự tăng theo thời gian | Chỉ báo thành công khi server xác nhận; thông báo yêu cầu quét đang chờ xử lý |
| Vừa | Coin cũ tải chậm ghi đè coin vừa chọn; liên kết Unicode bị bỏ qua | Kiểm tra thứ tự request, mã hóa symbol, xử lý hash sau khi đăng nhập |
| Vừa | Upsert CSV lỗi cú pháp SQL | Hỗ trợ table function và SELECT, escape đường dẫn và tên cột, giữ file gốc khi lỗi |
| Vừa | Thiếu hai biến thể ablation và thiếu trường kết quả forward-test | Khôi phục biến thể và hợp đồng dữ liệu; giữ yêu cầu calibrator hợp lệ |
| Vừa | Kiểm thử cũ chưa theo hợp đồng calibrator/episode tracker/stash mới | Sửa dữ liệu kiểm thử tương ứng; calibration dùng dữ liệu trước cutoff |
| Vừa | Healthcheck scanner chỉ kiểm tra file tồn tại | Kiểm tra timestamp, trạng thái chạy và chu kỳ thất bại |
| Vừa | Triển khai chạy song song với CI và xóa thay đổi chưa commit | Chờ CI thành công, triển khai đúng SHA, dừng nếu server có thay đổi, chỉ fast-forward |
| Vừa | Updater trong Docker bật dù .git chỉ đọc và runtime thiếu công cụ build frontend | Tắt updater trong cấu hình Docker; dùng quy trình triển khai từ host |
| Nhỏ | CLI bỏ qua --port/--reload và bind mọi giao diện | Hỗ trợ cờ CLI, giữ cổng positional cũ, tôn trọng host cấu hình |
| Nhỏ | Menu Lab trống; kiểm định bị ẩn ở chế độ thường | Ẩn nhóm không có mục, cho phép đọc báo cáo kiểm định |

## Kiểm chứng

- Lần kiểm chứng ban đầu sau sửa ngày 11/09: **481/481 kiểm thử Python qua** ở local.
- Cổng phát hành P0 ngày 12/09 chạy sạch trong Linux image: **491 passed, 2 skipped, 0 failed**, 32 cảnh báo từ joblib/scikit-learn, trong 8 phút 47 giây. Hai bài skip cần kho dữ liệu lịch sử ngoài repository.
- Vòng P1 ngày 13/09: **505 passed, 3 skipped, 0 failed**; 3 bài skip là script vận hành chỉ chạy trên Linux. Browser smoke: **3/3 qua**.
- Ruff toàn dự án: qua.
- Pyright theo danh sách bắt buộc của CI: 0 lỗi. Pyright toàn bộ backend vẫn còn **419 lỗi và 1 cảnh báo** trong các module pandas/Streamlit legacy; đây là nợ kỹ thuật, không được trình bày như toàn repository đã type-clean.
- Frontend TypeScript/Vite build và bundle budget: qua. `MainWorkspace` còn khoảng 183 KiB raw; chunk lớn nhất khoảng 475 KiB raw/154 KiB gzip, dưới gate 500/170 KiB.
- Docker builder và runtime image: build thành công với `libatomic1`/`libgomp1`.
- Runtime smoke test: chạy bằng `uid=1001/gid=1004`; LightGBM import thành công; frontend và `/api/health` trả 200; `/api/ready` trả 503 khi thiếu heartbeat và 200 sau heartbeat hợp lệ; Docker health chuyển `healthy`.
- Các kiểm thử mới chạy HTTP thật trên loopback, không gọi dịch vụ gửi thông báo.
- Giao diện local đăng nhập được, tải dữ liệu và biểu đồ được.
- Trạng thái triển khai và kiểm tra sau cập nhật được ghi bổ sung khi hoàn tất.

## Bổ sung P0 — 12/09/2026

- Chỉ giữ một nguồn nội dung chuẩn: xóa 6 tệp `.orig` và 9 tệp `update_*.patch` đã lỗi thời; báo cáo trên giao diện chỉ tải từ `docs/research/` qua API.
- Loại bỏ dữ liệu giả/dự phòng khỏi bề mặt live: market regime lấy từ nến BTC thật hoặc trả `UNKNOWN`; drift trả `NOT_EVALUATED` khi thiếu mẫu; benchmark engine trả `unavailable` khi không có dữ liệu; telemetry thiếu thì hiển thị không khả dụng.
- Gắn phạm vi bằng chứng cho toàn bộ báo cáo lịch sử. Số liệu training trong model bundle không còn được diễn giải thành forward-test, ROI hoặc win rate production.
- Model live được khóa bằng model ID, calibrator và checksum; kiểm thử release tải estimator thật và xác minh toàn bộ bundle trước serving.
- Tách liveness/readiness, bổ sung kiểm tra heartbeat và dung lượng đĩa, monitor production định kỳ, triển khai đúng commit SHA và tắt self-update trong container.
- Chuẩn hóa vận hành non-root, backup/prune/restore, cài cron có kiểm thử và tài liệu runbook. Cron `git reset --hard` tự động không an toàn trên GCP đã được vô hiệu hóa; crontab cũ được lưu bản sao.

## Bổ sung P1 — 13/09/2026

- Chuẩn hóa cấu hình runtime theo YAML có phiên bản; API, scanner và tài liệu dùng cùng nguồn cấu hình production.
- API/AI analyst không còn tạo dữ liệu SHAP, xác suất mặc định, causal claim hoặc lời khuyên giao dịch khi thiếu evidence. Hợp đồng mới dùng `feature_drivers` và ghi rõ phương pháp attribution.
- Tách tải frontend theo tab/component, thêm bundle budget CI và browser smoke cho đăng nhập, chuỗi API release, scanner trigger và báo cáo audit.
- Thêm protocol forward-test bất biến cho đúng model live: khóa freeze-time cutoff, checksum metadata/model/calibrator, label/horizon, universe policy, event grouping và sample gates; ghi dataset/universe fingerprint, latency và trạng thái chi phí.
- CLI, React API và Streamlit dùng chung evaluator fail-closed. Model chưa có protocol hoặc dữ liệu chưa đủ gate nhận `metrics: null`; chưa có metric production nào được suy diễn từ training stats.

## Phương án phát triển tiếp

### P1 — Khóa an toàn phát hành và bằng chứng model

1. **Đổi toàn bộ thông tin xác thực từng xuất hiện trong mã/lịch sử Git:** SSH, Telegram, webhook và API key liên quan. Việc này cần chủ hệ thống cấp/đổi secret; sửa mã không thể thu hồi secret cũ.
2. **Chạy backup production đầu tiên và diễn tập restore có kiểm soát.** Script và cron đã sẵn sàng, nhưng lần upload/khôi phục thật cần xác nhận phạm vi và thời điểm bảo trì.
3. **Đã có nền tảng; đang tích lũy mẫu:** forward-test độc lập cho đúng model live `frozen_20260906_105716_bc3c369b` đã khóa cutoff, universe policy, checksum metadata/model/calibrator, label contract, event grouping, latency và chi phí. Metric vẫn bị giữ kín cho tới khi mọi sample gate đạt.
4. **Đang thực hiện:** các ranh giới release của API đã type-clean, nhưng chưa tách xong `api_server.py` thành router/service/schema và full-backend Pyright còn 419 lỗi.
5. **Hoàn tất cổng hiện tại:** `MainWorkspace` đã tách dynamic import, CI có ngân sách bundle và 3 browser smoke cho đăng nhập, API release, scanner và report.

### P2 — Độ tin cậy vận hành

1. Đưa log/health/readiness/disk/backup age vào một bảng SLO; cảnh báo theo tình trạng scanner, độ trễ dữ liệu, dung lượng và backup gần nhất.
2. Diễn tập rollback theo exact SHA và restore định kỳ; ghi RTO/RPO thực đo vào runbook.
3. Thay polling phân tán bằng một lớp client API có timeout, cancellation, retry/backoff và schema validation thống nhất.

### P3 — Nâng chất lượng mô hình

1. Xây baseline tái lập trên snapshot bất biến và so sánh challenger theo walk-forward/event-level, không tối ưu threshold trên test fold.
2. Chỉ bật Meta-labeling hoặc Drift Guardian trong serving sau khi có ablation độc lập và quality gate theo regime.
3. Theo dõi calibration, precision/recall, lead time và độ phủ theo model version; tự động fail closed khi artifact, dữ liệu hoặc drift evidence không hợp lệ.

Không kiểm thử gửi Telegram thật hoặc gọi LLM có tính phí trong đợt P0. Cấu hình, hợp đồng lỗi và các lớp bảo vệ đã được kiểm tra mà không thay đổi người nhận hay phát sinh chi phí.

## Tài liệu đối chiếu quy trình triển khai

- [GitHub: workflow_run và điều kiện conclusion](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows)
- [Docker Compose: chờ dịch vụ healthy](https://docs.docker.com/reference/cli/docker/compose/up/)
